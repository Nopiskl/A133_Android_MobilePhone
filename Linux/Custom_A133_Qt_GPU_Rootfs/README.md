# Notice

**Language:** English | [中文](README_CN.md)

The kernel needs GPU power-domain support, the GPU driver, and the `dc_sunxi.ko` display bridge module.

## Release

`Custom_A133_Qt_GPU_Rootfs` is no longer maintained in the Git repository as source files.

For future rootfs files, split archives, and related change notes, download them from this repository's GitHub Releases.
